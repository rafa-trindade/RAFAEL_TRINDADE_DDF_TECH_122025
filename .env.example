# ----------------------------------------------------------
# MINIO / S3 (REMOTE ENDPOINT)
# MinIO já provisionado e acessível na VPS
# ----------------------------------------------------------
S3_ENDPOINT=

# ----------------------------------------------------------
# S3 CREDENTIALS (MinIO)
# ----------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# ----------------------------------------------------------
# AWS REGION (required by S3 SDKs: DuckDB, boto3, etc.)
# ----------------------------------------------------------
AWS_DEFAULT_REGION=us-east-1

# --------------------------------------------------
# PostgreSQL (Docker)
# Banco sobe via Docker Compose, exposto na VPS
# --------------------------------------------------
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=kaggle_olist
POSTGRES_USER=
POSTGRES_PASSWORD=

# ----------------------------------------------------------
# API FERIADOS - INVERTEXTO
# ----------------------------------------------------------
INVERTEXTO_API_KEY=

# ----------------------------------------------------------
# GEMINI (GOOGLE AI)
# ----------------------------------------------------------
GEMINI_API_KEY=